apiVersion: v1
# Kind for service
kind: Service
metadata:
  # Name your service
  name: postgresdb
  labels:
    app: postgresdb
spec:
  # Pod to route service traffic  
  selector:
    app: postgresdb
  # Choose how to expose your service
  ports:
    # The port number to expose the service
    - port: 5433
  